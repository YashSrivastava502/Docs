pm2 delete all
pm2 save


sudo rm -rf /change_management_portal/*
sudo mkdir -p /change_management_portal/releases
sudo mkdir -p /change_management_portal/logs


node -v
npm -v
pm2 -v

sudo npm install -g pm2

ecosystem.config.js

module.exports = {
  apps: [
    {
      name: "change-portal",
      script: "npm",
      args: "start",
      cwd: "/change_management_portal/current",
      instances: "max",
      exec_mode: "cluster",
      env: {
        NODE_ENV: "production",
        PORT: 3000
      }
    }
  ]
}


azure-pipelines.yml

trigger:
  branches:
    include:
      - main

pool:
  name: SelfHostedPool

variables:
  app_root: "/change_management_portal"
  release_dir: "/change_management_portal/releases"

steps:

- checkout: self

- task: NodeTool@0
  inputs:
    versionSpec: '18.x'

- script: |
    npm install
  displayName: Install dependencies

- script: |
    npm run build
  displayName: Build Next.js

- script: |

    BUILD_ID=$(Build.BuildId)

    mkdir -p $(release_dir)/build_$BUILD_ID

    rsync -av --delete ./ $(release_dir)/build_$BUILD_ID/

  displayName: Create release version

- script: |

    cd $(release_dir)/build_$(Build.BuildId)

    npm install --production

  displayName: Install production deps

- script: |

    ln -sfn $(release_dir)/build_$(Build.BuildId) \
    $(app_root)/current

  displayName: Switch to new version

- script: |

    cd $(app_root)/current

    if pm2 list | grep change-portal; then
        pm2 reload ecosystem.config.js
    else
        pm2 start ecosystem.config.js
    fi

    pm2 save

  displayName: Start or reload PM2

- script: |

    sleep 10

    STATUS=$(curl -o /dev/null -s -w "%{http_code}" http://localhost:3000)

    if [ $STATUS -ne 200 ]; then
        echo "Health check failed"
        exit 1
    fi

  displayName: Health check



  git push origin main

  
sudo chown -R azureagent:azureagent /change_management_portal
