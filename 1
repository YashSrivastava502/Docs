from sqlalchemy import create_engine
from config import DB_CONNECTION_STRING

engine = create_engine(
    DB_CONNECTION_STRING,
    pool_pre_ping=True,
    pool_size=5,
    max_overflow=10,
    pool_recycle=1800,
    connect_args={"application_name": "capacity-system"}
)

def get_engine():
    return engine
